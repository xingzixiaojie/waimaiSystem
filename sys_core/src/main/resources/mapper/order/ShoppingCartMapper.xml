<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.core.order.mapper.ShoppingCartMapper">

    <sql id="All_Column_List">
        id, name, image, user_id, dish_id, setmeal_id, dish_flavor, number, amount, create_time
    </sql>

    <!--  新增购物车里的商品  -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.chen.core.order.entity.ShoppingCartDO">
        INSERT INTO shopping_cart ( name, image, user_id, dish_id, setmeal_id, dish_flavor, number, amount, create_time)
        VALUES (#{name}, #{image}, #{userId}, #{dishId}, #{setmealId}, #{dishFlavor}, #{number}, #{amount}, #{createTime})
    </insert>

    <!--  根据id修改商品数量  -->
    <update id="updateNumberById">
        UPDATE shopping_cart
        SET number = #{number}
        WHERE id = #{id}
    </update>

    <!--  根据用户id删除所有商品  -->
    <delete id="deleteByUserId" parameterType="java.lang.Long">
        DELETE FROM shopping_cart
        WHERE user_id = #{userId}
    </delete>

    <!--  根据id删除商品  -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM shopping_cart
        WHERE id = #{id}
    </delete>

    <!--  查询购物车里的全部商品信息  -->
    <select id="list" resultType="com.chen.core.order.entity.ShoppingCartDO">
        SELECT <include refid="All_Column_List"/>
        FROM shopping_cart
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="dishId != null">
                AND dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                AND setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null and dishFlavor != '' ">
                AND dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

</mapper>
