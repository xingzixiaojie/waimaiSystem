<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.core.order.mapper.OrderDetailMapper">

    <sql id="All_Column_List">
        id, name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount
    </sql>

    <!--  批量插入订单明细数据  -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id" parameterType="com.chen.core.order.entity.OrderDetailDO">
        INSERT INTO order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.image}, #{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor}, #{item.number}, #{item.amount})
        </foreach>
    </insert>

</mapper>
